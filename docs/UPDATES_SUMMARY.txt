DOCUMENTATION UPDATES SUMMARY
Date: March 1, 2026

Objective
Update all README/documentation files so they describe the current `main` branch implementation, not the older scraper-led flow.

Result
Completed. The docs now match the app's current Yahoo OAuth, React-first, session-backed behavior.

Key Fixes Applied
1. Runtime flow
- Replaced "Connect launches the scraper" language with the actual flow:
  - set league ID in session
  - redirect to Yahoo OAuth when needed
  - refresh via `src/fba/yahoo_api.py`
  - cache standings in memory

2. Data persistence
- Clarified that `POST /refresh` does not automatically update `data/standings.json`.
- Documented the remaining legacy/manual uses of:
  - `data/config.json`
  - `data/standings.json`
  - `data/oauth2.json`
  - `data/browser_state.json`

3. Route coverage
- Added missing auth endpoints to the docs.
- Kept the API route list aligned with `src/fba/app.py`.

4. UI behavior
- Kept React mode documented as the default.
- Kept `FBA_UI_MODE=legacy` documented as the template fallback.
- Preserved React URL state behavior for analysis and games-played pages.

5. Verification
- Recorded the current passing command:
  - `./venv/bin/pytest -q tests/test_normalize.py tests/test_category_targets.py tests/test_cluster_leverage.py tests/test_games_played.py`
  - result: `120 passed`
- Recorded the current blocker on the checked-in `venv` for the full suite:
  - missing backend packages such as `python-dotenv` and `flask-login`

Constraint Compliance
- No non-documentation files were edited.
